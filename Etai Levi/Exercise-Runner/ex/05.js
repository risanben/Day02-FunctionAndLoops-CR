// Read 3 digits from the user, join them to one number and print it:
// for example: if the user entered the numbers 3,2,6, we should store them in a
// variable holding the value of ‘326’ and then print that variable to the console.

var digit1 = 1
var digit2 = 2
var digit3 = 3

var constructedNum = digit1 * 100 + digit2 * 10 + digit3;
console.log('The number constructed by the given digits is:', constructedNum);
